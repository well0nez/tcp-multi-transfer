name: release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.label }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            label: linux
          - os: windows-latest
            label: windows
          - os: macos-latest
            label: macos

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Build (release)
        run: cargo build --release

      - name: Prepare dist (unix)
        if: runner.os != 'Windows'
        run: |
          mkdir -p dist
          cp target/release/tcp-multi-transfer dist/
          cp run_server.py dist/
          cp README.md dist/

      - name: Prepare dist (windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dist | Out-Null
          Copy-Item target\release\tcp-multi-transfer.exe dist\
          Copy-Item run_server.py dist\
          Copy-Item README.md dist\

      - name: Create archive (unix)
        if: runner.os != 'Windows'
        run: |
          python - <<'PY'
          import os
          import zipfile

          label = "${{ matrix.label }}"
          archive = f"tcp-multi-transfer-{label}.zip"
          base_dir = "dist"

          with zipfile.ZipFile(archive, "w", zipfile.ZIP_DEFLATED) as zf:
              for root, _dirs, files in os.walk(base_dir):
                  for name in files:
                      path = os.path.join(root, name)
                      rel = os.path.relpath(path, base_dir)
                      zf.write(path, rel)
          PY

      - name: Create archive (windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          @'
          import os
          import zipfile

          label = "${{ matrix.label }}"
          archive = f"tcp-multi-transfer-{label}.zip"
          base_dir = "dist"

          with zipfile.ZipFile(archive, "w", zipfile.ZIP_DEFLATED) as zf:
              for root, _dirs, files in os.walk(base_dir):
                  for name in files:
                      path = os.path.join(root, name)
                      rel = os.path.relpath(path, base_dir)
                      zf.write(path, rel)
          '@ | python -

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: tcp-multi-transfer-${{ matrix.label }}
          path: tcp-multi-transfer-${{ matrix.label }}.zip

  release:
    name: Publish release
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**/tcp-multi-transfer-*.zip
          generate_release_notes: true
